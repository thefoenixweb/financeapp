//we export a type (user) to format data in other file

export type UserProfileToken = {
    userName: string;
    email : string;
    token : string;
}

export type UserProfile = {

    userName: string;
    email: string;
}